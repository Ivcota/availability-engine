type AppConfig = {
  appName: string;
  appDescription: string;
  domainName: string;
};

const config: AppConfig = {
  appName: "IvcotaStack",
  appDescription:
    "The boilerplate for IvcotaStack with all you need to build your SaaS, AI tool, or any other web app.",
  domainName: "ivcotastack.app",
};

export default config;
